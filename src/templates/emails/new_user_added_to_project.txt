{% extends "emails/base.txt" %}
{% block body %}
{{project_profile.profile.email}} was added to {{project_profile.project.name}} by {% if admin_profile %}{{admin_profile.full_name}} ({{admin_profile.email}}){% else %}a superadmin
  not using the API{% endif %}.
{% endblock %}
