{% extends "emails/base.txt" %}
{% block body %}
{{project_profile.profile.full_name}} ({{project_profile.profile.email}}) was added to {{project_profile.project.name}} by {% if profile %}{{admin_profile.full_name}} ({{admin_profileprofile.email}}){% else %}a superadmin
  not using the API{% endif %}.
{% endblock %}
